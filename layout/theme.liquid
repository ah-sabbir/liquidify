<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>{{ page_title }} - {{ shop.name }}</title>
    {{ content_for_header }}
    <link href="{{ 'theme.css' | asset_url }}" rel="stylesheet">
  </head>
  <body>
    {% section 'header' %}

    {{ content_for_layout }}

    {% section 'footer' %}
  </body>
</html>
